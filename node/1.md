```
fetch(file)
    .then(response => response.json())
    .then(data => {
        if (Array.isArray(data)) {
            const length = data.length; // Sửa từ "lenght" thành "length"
            let start = 0;
            let end = Math.min(length, 10); // Đảm bảo không vượt quá độ dài mảng

            for (start; start < end; start++) {
                const obj = data[start]; // Lấy đối tượng tại chỉ số `start`
                const appear_obj = document.createElement("div");
                appear_obj.classList.add("asset"); // Sửa từ classList thành classList.add

                appear_obj.innerHTML = `
                    <img src="${obj.img}" alt="">
                    <div class="information">${obj.name}</div>
                    <div class="source">
                        <a href="${obj.download}" target="_blank"><i class="fa-solid fa-download"></i></a>
                        <a href="${obj.source}" target="_blank"><i class="fa-solid fa-link"></i></a>
                    </div>
                `;

                appear_position.appendChild(appear_obj); // Chèn `appear_obj` vào vị trí mong muốn
            }
        }
    })
    .catch(error => console.error('Error:', error)); // Bắt lỗi nếu có

```